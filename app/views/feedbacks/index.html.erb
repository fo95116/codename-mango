<h1>Feedbacks#index</h1>
<p>Find me in app/views/feedbacks/index.html.erb</p>
<a href="/writers">Go to writers index</a>

<%= link_to "narratives", narratives_path, id: "test" %>

<ul class="grid cs-style-2">
  <% @images.each do |image| %>
    <li>
      <figure>
       <img src="<%= image.url %>" alt="image <%= image.id %>"/>
       <figcaption>
        <h3></h3>
        <span></span>
        <a></a>

       </figcaption>
      </figure>
    </li>
  <% end %>
</ul>
